PARTITIONS=ada0
DISTRIBUTIONS="kernel.txz base.txz"

#!/bin/sh

echo "ifconfig_em0=DHCP" >> /etc/rc.conf
echo "ntpd_enable=YES"   >> /etc/rc.conf
echo "sshd_enable=YES"   >> /etc/rc.conf

export ASSUME_ALWAYS_YES="yes"
ansible_python_interpreter=python2.7

dhclient em0
pkg install ansible git && \
#pkg update && \
#pkg upgrade && \
#pkg install ansible python3 git && \
cd /tmp && \
git clone https://github.com/duckinator/dotfiles.git system-config && \
cd /tmp/system-config/ansible && \
ansible-playbook -i localhost freebsd-desktop.yml && \
rm -rf /tmp/system-config
