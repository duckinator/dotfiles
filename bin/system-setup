#!/bin/sh

SCRIPT_DIR=$(dirname $(readlink -f $0)) # Directory this script is in.
cd $SCRIPT_DIR/../ansible/

if [ $(uname) = "FreeBSD" ]; then
  EXE_DIR=/usr/local/bin
else
  EXE_DIR=/usr/bin
fi

$EXE_DIR/ansible-playbook -i localhost freebsd-desktop.yml
